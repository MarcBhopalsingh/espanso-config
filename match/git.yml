# Git commands

matches:
  - trigger: ":gct"
    replace: "git checkout"

  - trigger: ":gs"
    replace: "git status"

  - trigger: ":gcm"
    replace: "git commit -m \"\""

  - trigger: ":gac"
    replace: "git add . && git commit -m \"\""
